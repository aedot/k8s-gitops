---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: decluttarr
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: akeyless
  target:
    name: decluttarr-secret
    template:
      data:
        config.yaml: |-
          general:
            test_run: false
          jobs:
            remove_failed_imports:
              message_patterns:
                - Not a Custom Format upgrade for existing*
                - Not an upgrade for existing*
          instances:
            radarr:
              - base_url: http://radarr.downloads
                api_key: {{ .RADARR_API_KEY }}
            sonarr:
              - base_url: http://sonarr.downloads
                api_key: {{ .SONARR_API_KEY }}
          download_clients:
            qbittorrent:
              - base_url: http://qbittorrent.downloads
  dataFrom:
    - extract:
        key: radarr
    - extract:
        key: sonarr
