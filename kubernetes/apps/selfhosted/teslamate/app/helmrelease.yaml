# yaml-language-server: $schema=https://k8s-schemas.home-operations.com/helm.toolkit.fluxcd.io/helmrelease_v2.json
---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app teslamate
spec:
  chartRef:
    kind: OCIRepository
    name: teslamate
  interval: 30m
  dependsOn: []
  values:
    controllers:
      teslamate:
        annotations:
          reloader.stakater.com/auto: "true"
        pod:
          enableServiceLinks: false
          securityContext:
            runAsUser: 1000
            runAsGroup: 1000
            runAsNonRoot: true
            fsGroup: 1000
            fsGroupChangePolicy: OnRootMismatch
        containers:
          app:
            image:
              repository: teslamate/teslamate
              tag: 4.0.1@sha256:e77ec65007aafb40d736ed2c89675112fe8d960886ea9d7de3255f2b5360269f
            envFrom:
              - secretRef:
                  name: "{{ .Release.Name }}-secret"
            env:
              - name: TZ
                value: America/Los_Angeles
              - name: DISABLE_MQTT
                value: "false"
              - name: MQTT_HOST
                value: "mosquitto.dbms.svc.cluster.local"
              - name: DATABASE_USER
                value: *app
              - name: DATABASE_NAME
                value: *app
              - name: DATABASE_HOST
                valueFrom:
                  secretKeyRef:
                    name: teslamate-psql-app
                    key: host
              - name: DATABASE_PASS
                valueFrom:
                  secretKeyRef:
                    name: teslamate-psql-app
                    key: password
            resources:
              requests:
                memory: 250Mi
                cpu: 50m
              limits:
                memory: 500Mi
    service:
      app:
        ports:
          http:
            port: 4000
    route:
      app:
        hostnames: ["{{ .Release.Name }}.${SECRET_DOMAIN}"]
        parentRefs:
          - name: envoy-internal
            namespace: network
