---
apiVersion: v1
kind: ConfigMap
metadata:
  name: pgsql-cluster-gatus-ep
  labels:
    gatus.io/enabled: "true"
data:
  config.yaml: |
    endpoints:
      - name: pgsql-cluster
        group: guarded
        url: tcp://pgsql-cluster-rw.dbms.svc.cluster.local:5432
        interval: 1m
        ui:
          hide-url: true
          hide-hostname: true
        conditions:
          - "[CONNECTED] == true"
